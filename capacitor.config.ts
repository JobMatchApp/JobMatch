import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.jobmatch',
  appName: 'jobmatch',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
